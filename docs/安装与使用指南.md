# 煤焦交易数据采集技能 — 安装与使用指南

> 版本 0.5.0.0 · 适用于人工手动运行、OpenClaw 类智能体无人值守定时运行与 Claude Code 技能形态使用。

采集**焦煤(jm)、焦炭(j)、动力煤(zc)**三个品种的**期货与现货**数据，幂等写入本地 **SQLite**，支持**历史回补**与**每日增量**。主通道为开源的 [AKShare](https://akshare.akfamily.xyz/)，另有多个免登录公开现货源作为补充。

---

## 1. 能力概览

| 类别 | 采集内容 | 数据表 |
|---|---|---|
| 期货 | 历史日线 / 主力连续 | `futures_daily` |
| 期货 | 实时行情快照 | `futures_realtime` |
| 期货 | 现货价与基差 | `spot_basis` |
| 期货 | 持仓排名 | `position_rank` |
| 期货 | 交易所库存 | `inventory` |
| 现货 | 分地区现货/指数价（结构化：品种×地区类型×地区） | `spot_regional` |
| 现货 | 原始指数价（未分类留存） | `index_price` |
| 统计 | 跨地区统计（均价/极差/最低最高地 + `ALL` 汇总） | `spot_regional_stats` |

所有表均以业务主键唯一约束，**重复运行幂等去重**（`INSERT ... ON CONFLICT`）。表结构见 `db/schema.sql`。

---

## 2. 环境要求

- **Python ≥ 3.9**（仅用标准库 + 下列第三方包，无需打包/虚拟环境也可运行，但推荐用 venv 隔离）。
- 联网（AKShare 与各现货源均需访问公网）。
- 磁盘：SQLite 库与 `runs/` 报告目录，长期运行建议留几百 MB。

第三方依赖（`requirements.txt`）：

| 包 | 版本 | 用途 |
|---|---|---|
| akshare | ≥1.12 | 主数据通道（期货行情/基差/持仓/库存） |
| pandas | ≥1.5 | AKShare 返回的数据帧处理 |
| requests | ≥2.28 | 现货公开源 HTTP 抓取 |
| beautifulsoup4 | ≥4.11 | 现货页面 HTML 解析 |
| pytest | ≥7.0 | 测试（运行采集本身不需要） |

---

## 3. 安装

```bash
# 1) 进入项目目录
cd /path/to/煤炭和焦炭交易数据采集技能

# 2)（推荐）建虚拟环境
python3 -m venv .venv && source .venv/bin/activate

# 3) 安装依赖
pip install -r requirements.txt

# 4) 冒烟验证（跑一小类，几秒钟）
python run.py --mode daily --kind inventory --format text
```

看到 `采集完成 ... exit_code=0` 即安装成功。首次运行会自动在 `db/coal_data.db` 建库建表。

### OpenClaw 一键安装

面向 OpenClaw 类智能体的自助安装（clone 公开仓 → venv → 依赖 → 冒烟验证）：

```bash
curl -fsSL https://raw.githubusercontent.com/vinnyren/coal-coke-data-collector/main/scripts/openclaw-bootstrap.sh | bash
```

详见 [OpenClaw 一键安装](OpenClaw一键安装.md)。

---

## 4. 快速开始

```bash
# 首次：历史回补（拉全历史日线/基差等，耗时较长）
python run.py --mode backfill --start 2015-01-01

# 日常：每日增量（只补最新交易日，快）
python run.py --mode daily
```

- **回补**只需做一次；之后每个交易日跑一次**增量**即可。
- 首次回补建议分类别跑（见 `--kind`），便于观察进度、隔离失败。

---

## 5. 命令行参数

```
python run.py [--mode backfill|daily] [--kind all|futures|spot|rank|inventory|regional]
              [--start YYYY-MM-DD] [--format json|text]
```

| 参数 | 取值 | 默认 | 说明 |
|---|---|---|---|
| `--mode` | `backfill` / `daily` | `daily` | 回补拉全历史；增量只补最新交易日 |
| `--kind` | 见下表 | `all` | 只跑某一类采集器 |
| `--start` | `YYYY-MM-DD` | `2015-01-01` | 仅 `backfill` 生效，历史起点 |
| `--format` | `json` / `text` | `json` | stdout 输出格式（见 §7） |

`--kind` 取值：

| 值 | 包含的采集器 |
|---|---|
| `all` | 下列全部 |
| `futures` | 历史日线 + 实时行情 |
| `spot` | 现货价与基差 |
| `rank` | 持仓排名 |
| `inventory` | 交易所库存 |
| `regional` | 生意社 / CCTD / 全国煤炭交易中心 / 中国太原煤炭价格指数 + 跨地区统计 |

示例：

```bash
python run.py --mode daily --kind futures            # 只更期货
python run.py --mode daily --kind regional           # 只更现货多地与统计
python run.py --mode backfill --kind regional        # 回补现货历史（含 ctctc 周度历史）
```

---

## 6. 无人值守 / 定时运行

设计目标之一是**可被调度器或智能体无人值守周期运行**。核心约定：

### 退出码（最可靠的健康信号，契约恒为 `{0, 2, 3}`）

| 退出码 | 含义 | 处置 |
|---|---|---|
| `0` | 全部跑通（含当日 0 行的软失败 `empty`、已知外部限制 `skipped`） | 健康 |
| `3` | 有采集器异常（报告内 `status=error` 标明） | 部分失败，看 `results[].error` |
| `2` | 致命（DB 初始化 / 报告写出失败 / 任何逃逸异常） | 需人工介入，看 `error` 字段 |

> `skipped` 是「因已知外部限制主动跳过」的诚实状态（如 DCE 持仓排名被瑞数 WAF 拦截、
> 未装浏览器插件），**不计入 error、不触发 exit 3**，避免每日误报造成告警疲劳。
> 详见 §9 的 position_rank 浏览器通道。

> 程序在启动时已把 stdout 重配为 UTF-8，并在 `main()` 顶层兜底任何异常，确保退出码始终落在 `{0,2,3}`，不会因中文/无 locale 崩成 exit 1。

### crontab（每个交易日 17:30，收盘后）

```cron
30 17 * * 1-5 cd /PATH/TO/项目 && flock -n /tmp/coal.lock /usr/bin/python3 run.py --mode daily >> logs/cron.log 2>&1
```

- **推荐 17:30（CST）而非凌晨**：`daily` 以 UTC 当日为起点，凌晨（00:00–08:00 CST）会落到 UTC 前一日（幂等 upsert 下仅多回补一天，无正确性问题）。
- `flock -n` 防止上一轮未跑完与下一轮重叠（重叠会触发 SQLite `database is locked`，被误报为 exit 3）。
- 无 locale 的环境建议加 `LANG=C.UTF-8`（程序已有 UTF-8 兜底，此为双保险）。

完整 crontab 与 macOS launchd 示例见 `scripts/cron.example`。

### OpenClaw 类智能体调度

让调度器按周期派生会话，指令示例（不要交互式提问，自动完成并回报）：

> 在项目目录运行 `python run.py --mode daily --kind all`，然后读取 `runs/latest.json`，用中文总结各源 `status/rows`、`totals` 与退出码。若退出码非 0 或存在 `status=error`，明确标注"采集告警"并列出失败源与 error 摘要。

完整示例见 `scripts/openclaw-task.example.md`。

### 报告目录保留

每次运行写一个 `runs/run-<UTC时间戳>.json` 归档，长期累积。定期清理示例（保留 30 天）：

```bash
find "${COAL_RUNS_DIR:-runs}" -name 'run-*.json' -mtime +30 -delete
```

---

## 7. 运行报告

每次运行都会：

1. 向 **stdout** 打印一份报告（`--format json` 为单个可解析 JSON；`--format text` 为人类可读摘要）。
2. 写 **`runs/latest.json`**（权威机器输出，原子覆盖，含致命失败也写出）。
3. 写 **`runs/run-<UTC时间戳>.json`** 归档（同秒冲突自动追加 `-N`，不覆盖）。

> ⚠️ 解析优先用 `runs/latest.json`：采集库偶发向 stdout 打印可能干扰 JSON，而 `latest.json` 是原子写、结构稳定的权威来源。

报告字段：

```jsonc
{
  "started_at":  "2026-07-03T09:30:00+00:00",   // ISO8601 UTC
  "finished_at": "2026-07-03T09:31:05+00:00",
  "duration_ms": 65000,                          // 顶层墙钟耗时（钳位 ≥0）
  "mode": "daily",
  "kind": "all",
  "results": [                                   // 每个采集器一条
    {"name": "inventory", "status": "ok", "rows": 112, "error": null, "duration_ms": 7156}
  ],
  "totals": {"rows": 112, "ok": 1, "empty": 0, "skipped": 0, "error": 0},
  "exit_code": 0
}
```

- `status` 取值：`ok`（跑通且行数>0）/ `empty`（跑通但 0 行，软失败）/ `skipped`（因已知外部限制主动跳过，如反爬 WAF 需浏览器；非代码错误，不触发 exit 3）/ `error`（fetch 抛异常）。
- **顶层 `error` 字段仅在致命失败（`exit_code=2`）时出现**，为一段 error 摘要；成功/部分失败的报告不含该键。逐采集器的错误在 `results[].error`。

---

## 8. 环境变量

| 变量 | 作用 | 说明 |
|---|---|---|
| `COAL_DB_PATH` | 指定 SQLite 库位置 | 支持 `~`；未设置回退 `db/coal_data.db` |
| `COAL_RUNS_DIR` | 指定运行报告目录 | 支持 `~`；未设置回退 `runs/` |
| `LANG` / `PYTHONIOENCODING` | 建议在无 locale 的 cron/systemd 设为 UTF-8 | 程序已有 UTF-8 兜底，此为双保险 |

```bash
COAL_DB_PATH=~/data/coal.db COAL_RUNS_DIR=~/data/coal-runs python run.py --mode daily
```

---

## 9. 数据来源与容错

- **主通道 AKShare**（开源、合法、零成本）：聚合新浪/东财/交易所的期货行情、基差、持仓、库存。
- **补充现货源**（`sources/`，可插拔，单源失败不影响主数据）：
  - `web_cctd`：CCTD 煤价指数中心公开指数页，双写 `index_price`（原始）与 `spot_regional`（可分类）。
  - `web_100ppi`：生意社现货表全国价，过 `HW_CHECK` JS 反爬挑战。
  - `web_ncexc`：全国煤炭交易中心 JSON 接口（直达煤=产地、下水煤=港口）。
  - `web_ctctc`：中国太原煤炭价格指数 JSON 接口，含 2023 至今周度历史。
- **失败隔离**：每个采集器在 `BaseCollector.run()` 内独立捕获异常并标 `status=error`，不中断其它采集器；网络类调用带 `with_retry` 重试。

### position_rank 浏览器通道（大商所瑞数 WAF）

大商所（DCE）全站升级了**瑞数动态安全 WAF**：对非浏览器请求返回 HTTP 412 + 动态混淆 JS 挑战页，放行 token 由 JS 在真实浏览器里动态生成，纯 `requests`（含补齐 headers/cookie）**无法绕过**，akshare 的裸请求会因此拿到 HTML 而抛 `BadZipFile`。

本项目改由**可选的无头浏览器通道**（`sources/dce_browser.py`）过挑战拿持仓排名 zip，再复用 akshare 的解析逻辑：

- **默认未装浏览器**：position_rank 标 `status=skipped` 并说明「需浏览器渲染 + 如何启用」，退出码保持 `0`，不影响其它 9 个采集器。
- **启用真实抓取**（可在无人值守中每日拿到持仓排名）：

  ```bash
  pip install playwright && playwright install chromium
  # 本机端到端实测（只读，不写主库）：
  .venv/bin/python scripts/verify_dce_browser.py 2026-07-03
  # 通过后照常运行即可，浏览器通道默认开启：
  .venv/bin/python run.py --mode daily --kind rank
  ```

- **开关**：`COAL_POSITION_RANK_BROWSER=0` 强制关闭浏览器通道（回到 `skipped` 降级）。
- **说明**：无头浏览器在无人值守 cron 中比纯 HTTP 脆弱（浏览器升级、超时、瑞数持续对抗），建议先用 `verify_dce_browser.py` 在目标机器上确认可用再纳入调度。

---

## 10. 常见问题排错

| 现象 | 可能原因 | 处理 |
|---|---|---|
| `exit_code=3`，某源 `status=error` | 网络波动 / 上游接口变动 / 反爬 | 看 `results[].error`；单源失败不影响其它，可重跑该 `--kind` |
| `position_rank` 为 `status=skipped` | DCE 瑞数 WAF + 未装浏览器插件 | 正常降级（非错误）；如需持仓排名见 §9「position_rank 浏览器通道」装 playwright |
| 装了 playwright 仍 `skipped` | 未 `playwright install chromium` / 本机无法访问 dce.com.cn / 瑞数挑战未过 | 跑 `scripts/verify_dce_browser.py` 看具体原因 |
| `exit_code=2`，`error` 含 `build_store failed` | DB 路径不可写 / 磁盘满 | 检查 `COAL_DB_PATH` 目录权限与磁盘 |
| `exit_code=2`，`error` 含 `write_report failed` | `runs/` 目录不可写 | 检查 `COAL_RUNS_DIR` 权限 |
| 某源 `status=empty`（0 行） | 当日无新数据 / 非交易日 | 通常正常（软失败），持续多日为空再排查 |
| SQLite `database is locked` | 两次运行重叠 | 用 `flock` 串行化（见 §6） |
| cron 下中文乱码/崩溃 | 环境无 UTF-8 locale | 程序已兜底；仍建议设 `LANG=C.UTF-8` |
| 完整异常堆栈在哪看 | 报告只留限长摘要 | 见 `logs/collector.log`（含完整 traceback） |

---

## 11. 测试

```bash
python -m pytest            # 全量单元测试
python -m pytest -q         # 简洁输出
```

覆盖存储幂等、重试/异常隔离、各采集器与解析器、地区分类、跨地区统计、报告构建/退出码/原子写、入口编排与 CLI。

---

## 12. 作为 Claude Code 技能使用

本项目以 Claude Code 技能形态交付：**仓库根目录即技能目录**（`SKILL.md` 在仓根，采集脚本、数据库、运行报告都在同一目录）。装入个人技能目录后，当用户提到"更新/采集煤焦数据、煤价、焦煤/焦炭/动力煤行情、库存/基差/持仓/分地区现货，或查询本地煤焦数据库"时，技能会被自动路由触发。触发描述见 `SKILL.md` frontmatter。

### 从 GitHub 安装

三种方式任选其一（效果相同）：

```bash
# 方式一（推荐）：clone 直接落到个人技能目录，再幂等安装
git clone https://github.com/vinnyren/coal-coke-data-collector.git \
  "$HOME/.claude/skills/coal-coke-data-collector"
bash "$HOME/.claude/skills/coal-coke-data-collector/scripts/install.sh"

# 方式二：curl 一键（复用 OpenClaw bootstrap，用 COAL_HOME 指定安装位置；
# 已存在则自动 git pull 更新后重装）
curl -fsSL https://raw.githubusercontent.com/vinnyren/coal-coke-data-collector/main/scripts/openclaw-bootstrap.sh \
  | COAL_HOME="$HOME/.claude/skills/coal-coke-data-collector" bash

# 方式三（开发者）：已有本地 clone，软链接入，改动即时生效
ln -sfn /path/to/coal-coke-data-collector "$HOME/.claude/skills/coal-coke-data-collector"
```

`install.sh` 是幂等的：建/复用 `.venv` → 装依赖 → 跑离线冒烟测试（pytest，全绿视为安装通过），失败会明确报错。要求 Python ≥ 3.9 与 git。

### 验证安装

1. **发现**：开新会话（或重启 Claude Code），技能列表应出现 `coal-coke-data-collector`。
2. **触发**：对 Claude 说"更新一下今天的煤焦数据"——它应自动使用本技能，用仓内 venv 运行 `run.py --mode daily --kind all`，读取 `runs/latest.json` 后按中文回报各源状态、totals 与退出码。
3. 首次使用建议让 Claude 执行一次历史回补（`--mode backfill`），之后每日增量即可。

### 更新与卸载

- **更新**：在技能目录 `git pull`（方式二重跑同一条命令即可）；依赖有变化时重跑 `scripts/install.sh`。
- **卸载**：删除 `~/.claude/skills/coal-coke-data-collector`（目录或软链）。注意 SQLite 数据库默认在技能目录内（`db/coal_data.db`），卸载前如需保留数据请先备份，或平时就用 `COAL_DB_PATH` 把库放在技能目录外。

---

## 相关文档

- `README.md` — 一页速览
- `SKILL.md` — 技能清单与无人值守约定
- `scripts/cron.example` — crontab / launchd 定时示例
- `scripts/openclaw-task.example.md` — OpenClaw 调度任务示例
- `docs/发布流程.md` — 版本发布流程（判级、CHANGELOG、`scripts/release.sh`）
- `CHANGELOG.md` — 版本变更日志
- `db/schema.sql` — 数据表结构
